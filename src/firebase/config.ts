// ─── Firebase config (not initialized in demo mode) ─────────
// Swap in real credentials and remove the stubs below when ready.
export const auth = null as any;
export const db = null as any;
export const storage = null as any;
export default null;
