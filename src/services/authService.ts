// ─── Demo stubs – no Firebase needed ───────────────────────────────────────
export const signUp = async (_email: string, _password: string, _displayName: string): Promise<any> => null;
export const signIn = async (_email: string, _password: string): Promise<any> => null;
export const logOut = async (): Promise<void> => {};
